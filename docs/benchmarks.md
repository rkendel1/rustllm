# Benchmarks

Benchmark harness is planned for production-readiness milestone.
Current MVP includes latency histogram instrumentation (`aether_request_latency_seconds`) for empirical measurements under load.
